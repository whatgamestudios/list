# Neon connection string. Use the *pooled* one (hostname contains "-pooler") -
# each request may open its own connection, so pooling matters here.
DATABASE_URL=postgres://user:password@ep-example-12345-pooler.region.aws.neon.tech/dbname?sslmode=require
